<UserControl x:Class="WeMail.Contact.Views.AddContactView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
            xmlns:i ="http://schemas.microsoft.com/xaml/behaviors"
            xmlns:rules="clr-namespace:WeMail.Common.ValidationRules;assembly=WeMail.Common"
             xmlns:cvt="clr-namespace:WeMail.Common.Converts;assembly=WeMail.Common"
             Background="{DynamicResource MaterialDesignPaper}"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
            prism:ViewModelLocator.AutoWireViewModel="True"
            FontSize="16"
            Height="400" Width="400">
    <UserControl.Resources>
        <ResourceDictionary>
            <cvt:SexConverter x:Key="SexConverter" />
        </ResourceDictionary>
    </UserControl.Resources>
    <i:Interaction.Behaviors>
        <rules:ValidationErrorMappingBehavior HasValidationError="{Binding IsInvalid, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
    </i:Interaction.Behaviors>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Label Content="姓名：" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <TextBox Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="250" Text="{Binding Contact.Name}" />
        <Label Grid.Row="1" Content="邮箱：" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <TextBox Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="250">
            <TextBox.Text>
                <Binding Path="Contact.Email" Delay="1000" UpdateSourceTrigger="PropertyChanged" NotifyOnValidationError="True">
                    <Binding.ValidationRules>
                        <rules:MailRule />
                    </Binding.ValidationRules>
                </Binding>
            </TextBox.Text>
        </TextBox>
        <Label Grid.Row="2" Content="手机号：" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <TextBox Grid.Row="2" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="250">
            <TextBox.Text>
                <Binding Path="Contact.PhoneNumber" Delay="1000" UpdateSourceTrigger="PropertyChanged" NotifyOnValidationError="True">
                    <Binding.ValidationRules>
                        <rules:PhoneNumberRule />
                    </Binding.ValidationRules>
                </Binding>
            </TextBox.Text>
        </TextBox>
        <Label Grid.Row="3" Content="年龄：" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <TextBox Grid.Row="3" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="250" Text="{Binding Contact.Age}" />
        <Label Grid.Row="4" Content="性别：" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <TextBox Grid.Row="4" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="250" Text="{Binding Contact.Sex, Converter={StaticResource SexConverter}}" />
        <Button Grid.Row="5" Grid.ColumnSpan="2" Content="添加" Width="100" Height="25" Command="{Binding AddContactCommand}" />
    </Grid>
</UserControl>

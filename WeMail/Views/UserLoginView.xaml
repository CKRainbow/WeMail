<UserControl x:Class="WeMail.Views.UserLoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True"
             Height="350" Width="500"
             FontSize="14"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="{DynamicResource MaterialDesignPaper}"
             >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="8*"/>
            </Grid.ColumnDefinitions>
            <Label Content="账号：" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" Text="{Binding Account}" Name="TextBoxEmailAddress"  MinWidth="200" Height="25" Margin="5,0,5,0"/>
            <Label Grid.Row="1" Content="密码：" HorizontalAlignment="Right" VerticalAlignment="Center" />
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Password}"  Name="TextBoxPassword" MinWidth="200" Height="25"  Margin="5,0,5,0"/>
        </Grid>
        <StackPanel Grid.Row="3" HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal" Margin="5,5,5,5">
            <Button x:Name="BtnLogin" Content="登录" Height="30" Margin="5,0,5,0" Command="{Binding LoginCommand}"/>
            <Button x:Name="BtnCancel" Content="取消" Height="30" Margin="5,0,5,0" Command="{Binding CancelCommand}" />
        </StackPanel>
    </Grid>
</UserControl>

<Window x:Class="WeMail.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Style="{StaticResource MaterialDesignWindow}"
        Title="{Binding Title}" Height="600" Width="900" >
    <Window.Resources>
        <Style x:Key="ModuleItemStyle" TargetType="ListBoxItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Grid>
                            <TextBlock Text="{Binding ModuleName}" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="8*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ListBox Grid.Row="1" ItemsSource="{Binding Modules}" SelectedItem="{Binding ModuleInfo}" ItemContainerStyle="{StaticResource ModuleItemStyle}"/>
        <ContentControl Grid.Row="1" Grid.Column="1" prism:RegionManager.RegionName="ContentRegion" />
        <StackPanel Grid.Column="1" Orientation="Horizontal">
            <Button Width="150" Height="30" Content="OpenViewA" Command="{Binding OpenViewA}" />
            <Button Width="150" Height="30" Content="OpenViewB" Command="{Binding OpenViewB}" />
            <Button Width="150" Height="30" Content="GoBack" Command="{Binding GoBack}" />
            <Button Width="150" Height="30" Content="GoForward" Command="{Binding GoForward}" />
        </StackPanel>
    </Grid>
</Window>

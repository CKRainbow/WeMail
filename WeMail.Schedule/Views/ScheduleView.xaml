<UserControl x:Class="WeMail.Schedule.Views.ScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WeMail.Schedule.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True" >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="1" />
            <ColumnDefinition Width="8*" />
        </Grid.ColumnDefinitions>
        <StackPanel>
            <DatePicker x:Name="MyDatePicker" materialDesign:HintAssist.Hint="选择日期" ></DatePicker>
        </StackPanel>
        <Rectangle Grid.Column="1" Fill="White" Width="1" />
        <ContentControl Grid.Column="2" Content="{Binding ElementName=MyDatePicker,Path=Text}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="40"></ContentControl>
    </Grid>
</UserControl>

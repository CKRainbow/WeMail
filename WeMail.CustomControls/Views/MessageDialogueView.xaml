<UserControl x:Class="WeMail.CustomControls.Views.MessageDialogueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True"
             Height="500" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="messageTextBox" Grid.Row="1" Background="Transparent" Text="{Binding Message, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,0" >
            <Button Width="80" Height="30" Content="OK" Command="{Binding OkCommand}"/>
            <Button Width="80" Height="30" Content="Cancel" Margin="0,0,10,0" Command="{Binding CancelCommand}"/> 
        </StackPanel>
    </Grid>
</UserControl>
